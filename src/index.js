import React from 'react';
import ReactDOM from 'react-dom';

const Index = () => {
  return <div>Welcome to Sample Application for Node + React + Webpack + Babel</div>;
};

ReactDOM.render(<Index />, document.getElementById('root'));
